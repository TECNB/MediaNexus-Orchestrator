# 补充动漫导入整理阶段日志

## 背景

动漫整季磁力导入在整理文件时，目前逐文件重命名日志之后缺少阶段过渡提示，用户无法判断任务是否正在移动、删除或清理目录。

## 目标

- 在 MediaNexus-Orchestrator 的整理流程中增加用户可见的当前动作日志。
- 日志应说明批量重命名、批量移动、删除跳过文件、清理空目录等阶段开始和结束。
- 不改动现有任务/日志数据库结构。

## 非目标

- 不改变 OpenList 批量接口调用方式。
- 不新增逐文件事务或失败回滚。
- 不跑全项目校验命令。
